import networkx as nx


def iter_dags_from_mag(mag):
    pass


def is_markov_equivalent(dag_one, dag_two):
    """Check if two DAGs are Markov equivalent.

    Checks if the two graphs have the same skeleton
    and v-structures.

    Parameters
    ----------
    dag_one : CausalGraph
        First causal DAG.
    dag_two : CausalGraph
        Second causal DAG.
    """
    pass
